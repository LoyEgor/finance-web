# Copy to .env (gitignored) and fill in. Never commit real keys.
# Stage 1 (reconcile.py) needs NONE of these — they are for the API fetchers (Stage 2-3).

# Interactive Brokers — Flex Web Service (read-only reporting token)
IBKR_FLEX_TOKEN=
IBKR_FLEX_QUERY_ID=

# Binance — read-only API key ("Enable Reading" only; no trade/withdraw)
BINANCE_API_KEY=
BINANCE_API_SECRET=
